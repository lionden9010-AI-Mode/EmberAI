# Local and cloud AI

Local mode keeps model inference and stored conversations on-device where technically possible. Cloud mode sends only the bounded generation context to the selected provider. Hybrid mode makes synchronization and research opt-in per story.
