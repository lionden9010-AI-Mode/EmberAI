# Contributing

Run `npm run check` and `npm run build` before opening a change. Keep domain, service, repository, API, and UI responsibilities separate. Do not commit secrets, provider credentials, generated binaries, or unresolved merge-conflict markers.
